export type StoredCompany={id:string;version:number;name:string;industry:string;city:string;phone:string;email:string;website?:string;owner:string;archived:boolean};
export type StoredContact={id:string;version:number;companyId:string;name:string;title?:string;email:string;phone:string;isPrimary:boolean;archived:boolean};
export type StoredOpportunity={id:string;version:number;companyId:string;leadId?:string;primaryContactId?:string;name:string;stage:string;value:number;probability:number;closeDate?:string;owner:string;priority:string;outcome?:string;archived:boolean};

export function mapCompany(row:Record<string,unknown>):StoredCompany{return {id:String(row.id),version:Number(row.version||1),name:String(row.name),industry:String(row.industry||''),city:String(row.city||''),phone:String(row.phone||''),email:String(row.email||''),website:row.website?String(row.website):undefined,owner:String(row.owner||''),archived:Boolean(row.archived)}}
export function mapContact(row:Record<string,unknown>):StoredContact{return {id:String(row.id),version:Number(row.version||1),companyId:String(row.company_id),name:String(row.name),title:row.title?String(row.title):undefined,email:String(row.email||''),phone:String(row.phone||''),isPrimary:Boolean(row.is_primary),archived:Boolean(row.archived)}}
export function mapOpportunity(row:Record<string,unknown>):StoredOpportunity{return {id:String(row.id),version:Number(row.version||1),companyId:String(row.company_id),leadId:row.lead_id?String(row.lead_id):undefined,primaryContactId:row.primary_contact_id?String(row.primary_contact_id):undefined,name:String(row.name),stage:String(row.stage),value:Number(row.value||0),probability:Number(row.probability||0),closeDate:row.close_date?String(row.close_date):undefined,owner:String(row.owner||''),priority:String(row.priority||'Medium'),outcome:row.outcome?String(row.outcome):undefined,archived:Boolean(row.archived)}}
